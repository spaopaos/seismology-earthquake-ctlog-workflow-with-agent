# MESS 后联合重定位实施检查点

依据：用户确认独立 PhaseNet+ CT、CC+CT 联合求解、仅三套目录，沿用原 HypoDD 空间证据规则。brainstorming 已完成需求与方案讨论；其引用的 writing-plans 子技能在本机可用技能目录中不存在，使用本项目检查点执行，不阻塞已授权的实现。

1. 保存受影响维护源码的旧版本身份；新增独立阶段和三档配置。
2. 实现独立拾取匹配、统一事件表、CC 时间/台站/深度适配及独立输入校验；ph2dt 生成 CT。
3. 扩展维护的 HypoDD runner 支持显式 IDAT=3、两类加权及输入身份，复用原空间证据选择；回归 IDAT=2。
4. 新阶段按三 CC 档执行转换、配对、试算、正式求解并发布契约/QC。保留 EMPTY 与 UNAVAILABLE。
5. 更新入口、配置、知识绑定和文档，维护新版本身份；不改原始 Wiki/PDF 或既有科学运行。
6. 运行转换/拒绝错误的测试、小型钉版原生 CC+CT 联合求解、旧 CT 回归、知识与契约检查；按实际证据报告范围。
